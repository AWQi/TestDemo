<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.dell.testdemo">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:label="Test"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true">

        <!-- 1  .... -->
        <!--
             <activity android:name=".CirCleTextView.CircleTextViewActivity">
         <intent-filter>
         <action android:name="android.intent.action.MAIN" />
         <category android:name="android.intent.category.LAUNCHER" />
         </intent-filter>
         </activity>
        -->
        <!-- 2  ...... -->
             <activity
         android:name=".floating.activity.MyActivity"
         android:label="@string/app_name">
         <intent-filter>
         <action android:name="android.intent.action.MAIN" />
         <category android:name="android.intent.category.LAUNCHER" />
         </intent-filter>
         </activity>
         <activity android:name=".floating.activity.LeftBottomActivity" />
         <activity android:name=".floating.activity.LeftTopActivity" />
         <activity android:name=".floating.activity.RightBottomActivity" />
         <activity android:name=".floating.activity.RightTopActivity" />
        <!-- 3....... -->
<!--            <activity
            android:name=".myanimation.MyAnimation"
            android:theme="@style/Theme.AppCompat">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>-->
        <!-- 4  ...... -->
 <!--       <activity
            android:name=".VideoImage.VideoImage"
            android:theme="@style/Theme.AppCompat">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>-->


        <!--  ...5....
        -->
    <!--    <activity android:name=".horseekbar.HorSeekBar"

            android:theme="@style/Theme.AppCompat.Light.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>-->
    </application>

</manifest>